import Layout from '../components/Layout'

// can it push notifications?

export default function Preferences() {
  return (
    <Layout>
      <h1>
        preferences: notifications? etc etc idk
      </h1>
    </Layout>
  )
}

